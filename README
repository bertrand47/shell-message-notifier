This is a simple hack to avoid missing IM messages. To install the extension
just type in a terminal "make install" and follow the instructions.

http://cgit.collabora.com/git/user/bari/shell-message-notifier.git/
